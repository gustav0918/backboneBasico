App.Model.UserModel = Backbone.Model.extend({
        defaults: {
            'firstName': '',
            'lastName': '',
            'date':''
        },
        initialize: function() {
 
        }
    });